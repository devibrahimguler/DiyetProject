import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
        margin: 10,
        justifyContent: "center",
        alignItems: "center",
    },
    image: {
        height: 120,
        width: 120,
        borderRadius: 40,
        borderWidth: 1,
    },
});