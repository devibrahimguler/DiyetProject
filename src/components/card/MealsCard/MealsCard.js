import React from "react";
import { Text, Image, TouchableOpacity } from "react-native";

const MealsCard = () => {
    return (
        <TouchableOpacity>
            <Text>MealsCard</Text>
        </TouchableOpacity>
    );
}

export default MealsCard;